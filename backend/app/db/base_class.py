# In SQLModel, SQLModel itself acts as the base class for your models.
# You typically don't need a separate base_class.py unless you have
# common SQLAlchemy Mixins or declarative bases beyond what SQLModel provides.