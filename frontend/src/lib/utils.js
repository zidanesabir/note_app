// Add any general utility functions here, e.g., date formatting, markdown rendering helpers.

// Example: Simple markdown to HTML conversion (requires a library like 'marked' or 'react-markdown')
// For Next.js, 'react-markdown' is often used directly in components.
export const formatMarkdown = (markdownText) => {
  // This is a placeholder. You'd typically use a library here.
  // E.g., import { marked } from 'marked'; return marked(markdownText);
  return markdownText; // Return as-is for now if no markdown library installed
};